<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Not Potions</title>

    <h1>Hello there!</h1>

    <a class="nav-link active" href="/notpotions/addpotion">Add a Potion</a>

    <% if (posts.length === 0 ) { %>
        <p>no posts yet</p>
      <% } %> 
   
      <% posts.forEach( post => { %>

        <li><%= post.img %></li>
        <li><%= post.name %></li>
        <li><%= post.brewTime %></li>
        <li><%= post.ingredients %></li>
        <li><%= post.directions %></li>
        <li><%= post.notes %></li>
 

     <a href='/notpotions/<%=post._id %>' class="btn btn-sucess">VIEW</a>
          
     <form action='/notpotions/<%=post._id %>?_method=DELETE' method="POST">
    <button type="submit" class="btn btn-danger">DELETE</button>
       </form>
    
         </ul>
         <% }) %>



</head>
<body>
    
</body>
</html>